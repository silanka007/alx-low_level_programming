#include "main.h"

/**
 * _abs - returns the absolute value of passed int argument
 * @n: passed argument of type integer
 * Return: integer
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (0 - n);
}
